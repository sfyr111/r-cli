import { handleActions } from 'redux-actions'
import { fromJS } from 'immutable'
import * as types from './{{ dashCase name }}.type'

const initState = fromJS({
  data: {}
})

export const {{ camelCase name }} = handleActions(
  {
    [types.{{ constantCase name }}_DATA_UPDATE]: (state, action) => state.set('data', action.payload),
  },
  initState
)
