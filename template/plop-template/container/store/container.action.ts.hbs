import { createAction } from 'redux-actions'
import * as types from './{{ dashCase name }}.type'

export const init{{ properCase name }}Saga = createAction(types.SAGA_{{ constantCase name }}_INIT)

export const update{{ properCase name }}Data = createAction(types.{{ constantCase name }}_DATA_UPDATE)

